
import path from "path";

const webConf = {
    
    config: {
        webConf: {
            port: 3000,
            loggerPath: "./log",
            logFileKeepDays: "1",
            defaultLanguage: "cn",
            title: "文档"
        }
    }

};

export default webConf;