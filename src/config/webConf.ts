
import path from "path";

const webConf = {
    
    config: {
        webConf: {
            port: 6080,
            loggerPath: "./log",
            logFileKeepDays: "1",
            defaultLanguage: "cn",
            title: "文档" 
        }
    }

};

export default webConf;